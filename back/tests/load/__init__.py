"""F11-T2: paquete de carga.

Contiene el script `shadow_agent_load.py` que simula N sesiones
concurrentes contra `POST /message` para medir la latencia del turno
principal con el Shadow Agent activo.
"""
